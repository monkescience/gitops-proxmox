apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: signoz
  namespace: observability
spec:
  parentRefs:
    - name: public
      namespace: istio-system
  rules:
    - backendRefs:
        - name: signoz
          port: 8080
